% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_reporter_status.R
\name{get_process_reporter_status}
\alias{get_process_reporter_status}
\title{Get process reporter status}
\usage{
get_process_reporter_status(hostname = NULL, con = NULL)
}
\arguments{
\item{hostname}{Check specific host (NULL = current host)}

\item{con}{Database connection (NULL = get default)}
}
\value{
Data frame with reporter status or NULL if not running
}
\description{
Checks if a process reporter is running on a specific host.
}
\examples{
\dontrun{
status <- get_process_reporter_status()
if (!is.null(status)) {
  cat("Reporter running, PID:", status$process_id, "\n")
}
}
}
