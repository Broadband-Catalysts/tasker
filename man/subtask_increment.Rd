% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subtask_increment.R
\name{subtask_increment}
\alias{subtask_increment}
\title{Atomically increment subtask item counter}
\usage{
subtask_increment(
  increment = 1,
  quiet = TRUE,
  conn = NULL,
  run_id = NULL,
  subtask_number = NULL
)
}
\arguments{
\item{increment}{Number of items to add to counter (default: 1)}

\item{quiet}{Suppress console messages (default: TRUE for parallel workers)}

\item{conn}{Database connection (optional)}

\item{run_id}{Run ID from task_start(), or NULL to use active context}

\item{subtask_number}{Subtask number, or NULL to use current subtask}
}
\value{
TRUE on success
}
\description{
This function atomically increments the items_complete counter for a subtask.
Unlike subtask_update() which sets the value, this function increments it,
making it safe for use by parallel workers.
}
\examples{
\dontrun{
# Old style - explicit parameters
subtask_increment(increment = 1, run_id = run_id, subtask_number = subtask_num)

# New style - use context
subtask_increment(increment = 1)  # Uses active context and current subtask
}
}
\seealso{
\code{\link[=subtask_update]{subtask_update()}} to set progress values (use increment for
parallel workers), \code{\link[=subtask_start]{subtask_start()}} to start tracking a subtask,
\code{\link[=export_tasker_context]{export_tasker_context()}} to share context with parallel workers,
\code{\link[=tasker_cluster]{tasker_cluster()}} for simplified parallel setup
}
