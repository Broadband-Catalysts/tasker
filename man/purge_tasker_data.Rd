% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/purge_tasker_data.R
\name{purge_tasker_data}
\alias{purge_tasker_data}
\title{Purge All Data from tasker Tables}
\usage{
purge_tasker_data(conn = NULL, confirmation_string = "DELETE ALL DATA")
}
\arguments{
\item{conn}{Optional database connection. If NULL, uses connection from config.}

\item{confirmation_string}{The confirmation string the user must type to proceed.
Default is "DELETE ALL DATA". Set to NULL to skip confirmation prompt
(useful for programmatic use, but be very careful!).}

\item{interactive}{If TRUE (default), prompts user for confirmation.
Set to FALSE for non-interactive scripts (requires confirmation_string = NULL).}
}
\value{
TRUE if successful, FALSE if cancelled
}
\description{
This function deletes all data from the tasker database tables.
It preserves the schema structure but removes all task registrations,
execution history, and progress tracking data.
}
\details{
\strong{WARNING}: This operation is irreversible. All task history and
progress data will be permanently deleted.
}
\examples{
\dontrun{
# Interactive mode (will prompt for confirmation)
purge_tasker_data()

# Programmatic mode (skips confirmation - USE WITH CAUTION!)
purge_tasker_data(confirmation_string = NULL, interactive = FALSE)
}
}
