% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_monitoring.R
\name{get_monitor_connection}
\alias{get_monitor_connection}
\title{Get or create database connection for monitoring}
\usage{
get_monitor_connection(config = NULL, session_con = NULL)
}
\arguments{
\item{config}{Configuration list (default: getOption("tasker.config"))}

\item{session_con}{Existing connection to reuse if valid (optional)}
}
\value{
DBI database connection
}
\description{
Creates a new database connection or returns an existing valid connection
for monitoring database queries. Supports PostgreSQL and MySQL databases.
}
\examples{
\dontrun{
config <- tasker_config()
con <- get_monitor_connection(config)
# ... use connection ...

# Reuse connection
con <- get_monitor_connection(config, con)
}
}
