% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remove_subtask.R
\name{remove_subtask}
\alias{remove_subtask}
\title{Remove Subtask Progress Records}
\usage{
remove_subtask(
  run_id = NULL,
  task_id = NULL,
  script_filename = NULL,
  subtask_number = NULL,
  conn = NULL,
  confirmation_string = "REMOVE SUBTASKS",
  interactive = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{run_id}{Run ID (UUID) to remove subtasks from, or NULL to specify by task}

\item{task_id}{Task ID to remove all subtask records from (ignored if run_id is provided)}

\item{script_filename}{Script filename to identify task (alternative to task_id)}

\item{subtask_number}{Optional specific subtask number to remove (NULL removes all)}

\item{conn}{Optional database connection. If NULL, uses connection from config.}

\item{confirmation_string}{The confirmation string the user must type to proceed.
Default is "REMOVE SUBTASKS". Set to NULL to skip confirmation prompt
(useful for programmatic use).}

\item{interactive}{If TRUE (default), prompts user for confirmation.
Set to FALSE for non-interactive scripts (requires confirmation_string = NULL).}

\item{quiet}{If TRUE, suppress informational messages (default: FALSE)}
}
\value{
Invisibly returns a list with removal details:
\item{subtasks_removed}{Number of subtask records removed}
\item{run_id}{Run ID if specified}
\item{task_id}{Task ID if specified}
}
\description{
Removes subtask progress records for a specific run or all runs of a task.
This is useful for cleaning up orphaned or test subtask records.
}
\details{
\strong{WARNING}: This will permanently remove subtask progress data.
Use with caution.
}
\examples{
\dontrun{
# Remove all subtasks for a specific run (interactive)
remove_subtask(run_id = "123e4567-e89b-12d3-a456-426614174000")

# Remove all subtasks for all runs of a task
remove_subtask(script_filename = "ONEOFF_01_Test_Script.R",
               confirmation_string = NULL, interactive = FALSE)

# Remove specific subtask number from a run
remove_subtask(run_id = "123e4567-e89b-12d3-a456-426614174000",
               subtask_number = 2,
               confirmation_string = NULL, interactive = FALSE)
}
}
