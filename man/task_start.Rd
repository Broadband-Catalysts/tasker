% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task_start.R
\name{task_start}
\alias{task_start}
\title{Start tracking a task execution}
\usage{
task_start(
  stage,
  task,
  total_subtasks = NULL,
  message = NULL,
  version = NULL,
  git_commit = NULL,
  quiet = FALSE,
  conn = NULL,
  .active = TRUE
)
}
\arguments{
\item{stage}{Stage name}

\item{task}{Task name}

\item{total_subtasks}{Total number of subtasks (optional)}

\item{message}{Initial progress message (optional)}

\item{version}{Version string (optional)}

\item{git_commit}{Git commit hash (optional)}

\item{quiet}{Suppress console messages (default: FALSE)}

\item{conn}{Database connection (optional)}

\item{.active}{Set this run as the active context (default: TRUE).
When TRUE, subsequent tasker function calls can omit the run_id parameter.
Set to FALSE if starting a task within a library function to avoid
overwriting the user's active context.}
}
\value{
run_id (UUID) to track this execution
}
\description{
Start tracking a task execution
}
\examples{
\dontrun{
# Old style - explicit run_id
run_id <- task_start("STATIC", "Process FCC Data")

# New style - automatic context (run_id optional in subsequent calls)
task_start("STATIC", "Process FCC Data")
subtask_start("Load files")  # No run_id needed!
subtask_complete()
task_complete()
}
}
\seealso{
\code{\link[=task_update]{task_update()}} to update task status, \code{\link[=task_mark_complete]{task_mark_complete()}} to
mark task complete, \code{\link[=task_reset]{task_reset()}} to reset task state,
\code{\link[=subtask_start]{subtask_start()}} to start tracking subtasks, \code{\link[=get_task_status]{get_task_status()}} to
query task status
}
